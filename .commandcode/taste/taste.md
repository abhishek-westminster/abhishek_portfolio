# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# theme
- Use Tailwind CSS theme variables (`bg-background`, `text-foreground`, `text-muted-foreground`, `bg-card`, `bg-muted`) instead of hardcoded color values in components to support dark/light theme toggling. Confidence: 0.65

